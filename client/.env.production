# Since frontend and backend are on the same Vercel domain,
# we use a relative /api path — no need for an absolute URL.
# The api.js fallback `|| '/api'` handles this automatically.
VITE_API_URL=https://medusa-portfolio.vercel.app/api